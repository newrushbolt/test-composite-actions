---
name: Build go binaries
description: ""
author: 'newrushbolt'
inputs:
  github_token:
    description: 'GITHUB_TOKEN'
    default: '${{ github.token }}'
runs:
  using: "composite"
  # container:
  #   image: quay.io/prometheus/golang-builder:1.23-base
  # strategy:
  #   matrix:
  #     GOARCH: [arm64, amd64]
  # env:
  #   GOARCH: ${{ matrix.GOARCH }}
  steps:
    steps:
      - run: echo "go build -o ${GOARCH}"
      - run: echo "upload ${GOARCH}"
